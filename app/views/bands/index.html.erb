<h1>This is the list of all bands</h1>

<% @bands.each do |band| %>
  <a class="band_link" href="<%= band_url(band) %>"><%= band.name %></a>
  <br>
<% end %>

<a class= "create_band" href="<%= new_band_url %>">Create a band</a>
